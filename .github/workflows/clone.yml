name: Clone Backup Repositories

on:
  schedule:
    - cron: "0 0 */3 * *"
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          # this might remove tools that are actually needed,
          # if set to "true" but frees about 6 GB
          tool-cache: true
          
          # all of these default to true, but feel free to set to
          # "false" if necessary for your workflow
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true
          
      - name: Install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y jq yq gh git curl p7zip-full coreutils

      - name: Run backup
        env:
          GITHUB_TOKEN: ${{ secrets.MY_TOKEN }}
        run: |
          set -euo pipefail
          export GIT_TERMINAL_PROMPT=0

          NOW_DATE=$(date +"%Y%m%d")
          NOW_TIME=$(date +"%H%M")
          NOW_HUMAN=$(date +"%Y-%m-%d %H:%M")

          sanitize() {
            echo "$1" \
              | tr '/' '-' \
              | tr '[:upper:]' '[:lower:]' \
              | sed 's/[^a-z0-9._-]/-/g' \
              | sed 's/-\+/-/g' \
              | sed 's/^-//;s/-$//'
          }

          cleanup() {
            rm -rf tmp *.7z* *.sha256 2>/dev/null || true
          }

          auth_url() {
            echo "https://x-access-token:${GITHUB_TOKEN}@github.com/$1.git"
          }

          fetch_repo_info() {
            curl -fsSL \
              -H "Authorization: Bearer ${GITHUB_TOKEN}" \
              "https://api.github.com/repos/$1" \
              2>/dev/null || echo '{}'
          }

          fetch_latest_release() {
            curl -fsSL \
              -H "Authorization: Bearer ${GITHUB_TOKEN}" \
              "https://api.github.com/repos/$1/releases/latest" \
              2>/dev/null || echo '{}'
          }

          release_exists() {
            gh release view "$1" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1
          }

          human_size() {
            numfmt --to=iec --suffix=B "$1" 2>/dev/null || echo "${1} B"
          }

          build_notes() {
            REPO="$1"
            REPO_JSON="$2"
            BRANCH_COUNT="$3"
            ARCHIVE_SIZE="$4"
            RELEASE_TIME="$5"
            UPSTREAM_JSON="$6"

            REPO_URL=$(echo "$REPO_JSON" | jq -r '.html_url')
            DESCRIPTION=$(echo "$REPO_JSON" | jq -r '.description // "no description"')
            DEFAULT_BRANCH=$(echo "$REPO_JSON" | jq -r '.default_branch')
            LANGUAGE=$(echo "$REPO_JSON" | jq -r '.language // "unknown"')
            STARS=$(echo "$REPO_JSON" | jq -r '.stargazers_count')
            FORKS=$(echo "$REPO_JSON" | jq -r '.forks_count')
            PRIVATE=$(echo "$REPO_JSON" | jq -r '.private')

            CREATED_AT=$(echo "$REPO_JSON" | jq -r '.created_at')
            UPDATED_AT=$(echo "$REPO_JSON" | jq -r '.updated_at')
            PUSHED_AT=$(echo "$REPO_JSON" | jq -r '.pushed_at')

            UP_TAG=$(echo "$UPSTREAM_JSON" | jq -r '.tag_name // "none"')
            UP_NAME=$(echo "$UPSTREAM_JSON" | jq -r '.name // "none"')
            UP_TIME=$(echo "$UPSTREAM_JSON" | jq -r '.published_at // "none"')

            printf "## 📦 Repository Info\n"
            printf "- URL: %s\n" "$REPO_URL"
            printf "- Description: %s\n" "$DESCRIPTION"
            printf "- Default branch: %s\n" "$DEFAULT_BRANCH"
            printf "- Language: %s\n" "$LANGUAGE"
            printf "- Stars: %s | Forks: %s\n" "$STARS" "$FORKS"
            printf "- Private: %s\n\n" "$PRIVATE"

            printf "## 🕒 Source Timeline\n"
            printf "- Created: %s\n" "$CREATED_AT"
            printf "- Updated: %s\n" "$UPDATED_AT"
            printf "- Pushed: %s\n\n" "$PUSHED_AT"

            printf "## 🏷 Upstream Release\n"
            printf "- Tag: %s\n" "$UP_TAG"
            printf "- Name: %s\n" "$UP_NAME"
            printf "- Published: %s\n\n" "$UP_TIME"

            printf "## 📊 Backup Info\n"
            printf "- Branch count: %s\n" "$BRANCH_COUNT"
            printf "- Archive size: %s\n" "$(human_size "$ARCHIVE_SIZE")"
            printf "- Backup time: %s\n" "$RELEASE_TIME"
          }

          backup_repo() {
            REPO="$1"

            echo "==== BACKUP: $REPO ===="

            cleanup
            mkdir -p tmp

            REPO_SLUG=$(sanitize "$REPO")
            TAG="${REPO_SLUG}-${NOW_DATE}-${NOW_TIME}"
            TAG=$(sanitize "$TAG")

            if release_exists "$TAG"; then
              echo "Skip: $TAG"
              return 0
            fi

            REPO_JSON=$(fetch_repo_info "$REPO")
            UPSTREAM_JSON=$(fetch_latest_release "$REPO")

            MIRROR="tmp/${REPO_SLUG}.git"
            EXPORT="tmp/export/${REPO_SLUG}"

            git clone --mirror "$(auth_url "$REPO")" "$MIRROR" >/dev/null 2>&1 || {
              echo "clone failed"
              return 0
            }

            mapfile -t BRANCHES < <(
              git -C "$MIRROR" for-each-ref --format='%(refname:short)' refs/heads
            )

            mkdir -p "$EXPORT"

            FILES=()
            IDX=0

            for B in "${BRANCHES[@]}"; do
              DIR="${EXPORT}/$(sanitize "$B")"
              mkdir -p "$DIR"

              git -C "$MIRROR" archive "$B" | tar -xf - -C "$DIR"

              ARCHIVE="${REPO_SLUG}-part-${IDX}.7z"
              7z a -t7z -mx=5 -mmt=2 "$ARCHIVE" "$DIR" >/dev/null || true
              sha256sum "$ARCHIVE" > "$ARCHIVE.sha256"

              FILES+=("$ARCHIVE" "$ARCHIVE.sha256")
              IDX=$((IDX + 1))
            done

            BRANCH_COUNT="${#BRANCHES[@]}"
            TOTAL_SIZE=$(du -sb *.7z 2>/dev/null | awk '{s+=$1} END {print s+0}')

            NOTES_FILE=$(mktemp)
            build_notes "$REPO" "$REPO_JSON" "$BRANCH_COUNT" "$TOTAL_SIZE" "$NOW_HUMAN" "$UPSTREAM_JSON" > "$NOTES_FILE"

            # create release
            gh release create "$TAG" \
              "${FILES[0]}" \
              --repo "$GITHUB_REPOSITORY" \
              --title "$REPO_SLUG ($NOW_HUMAN)" \
              --notes-file "$NOTES_FILE"

            # upload remaining files into same release
            for ((i=1; i<${#FILES[@]}; i++)); do
              gh release upload "$TAG" "${FILES[$i]}" \
                --repo "$GITHUB_REPOSITORY" \
                --clobber
            done

            rm -f "$NOTES_FILE"
            cleanup
          }

          process_repos() {
            mapfile -t REPOS < <(yq -r '.repositories.repo0[]' repositories.yaml 2>/dev/null || true)
            for R in "${REPOS[@]}"; do
              backup_repo "$R"
            done
          }

          process_all() {
            mapfile -t OWNERS < <(yq -r '.allrepositories[]' repositories.yaml 2>/dev/null || true)

            for O in "${OWNERS[@]}"; do
              mapfile -t LIST < <(
                curl -fsSL \
                  -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                  "https://api.github.com/users/$O/repos?per_page=100" \
                | jq -r '.[] | select(.private==false) | .full_name'
              )

              for R in "${LIST[@]}"; do
                backup_repo "$R"
              done
            done
          }

          process_all
          process_repos
